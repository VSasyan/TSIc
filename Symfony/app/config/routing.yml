app:
    resource: "@AppBundle/Controller/"
    type:     annotation

accueil:
    path:      /
    defaults:  { _controller: AppBundle:Perturbation:listNearest }

logo_type_perturbation:
    path:      /file/logo-type-perturbation/{id}
    defaults:  { _controller: AppBundle:File:logoTypePerturbation }

#app_routing:
#    resource: "@AppBundle/Resources/config/routing.yml"
#    type:     yml

# Client-side routing
fos_js_routing:
    resource: "@FOSJsRoutingBundle/Resources/config/routing/routing.xml"
